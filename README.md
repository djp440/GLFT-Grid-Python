# GLFT--Grid-Python
基于GLFT 模型的动态网格策略

# 项目依赖
python => 3.13.5

复制env copy.md文件并重命名为.env，在其中填写api信息

```
git clone https://github.com/djp440/GLFT-Grid-Python.git
cd GLFT-Grid-Python
pip install -r requirements.txt
```